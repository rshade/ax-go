{
  "name": "ax-go",
  "version": "0.0.0",
  "description": "Tooling devDependencies (commitlint, markdownlint) for the ax-go library",
  "private": true,
  "scripts": {
    "lint:md": "markdownlint AGENTS.md README.md \"docs/**/*.md\"",
    "commitlint": "commitlint"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.0",
    "@commitlint/config-conventional": "^21.0.0",
    "markdownlint-cli": "^0.48.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "Apache-2.0"
}
